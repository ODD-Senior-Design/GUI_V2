# ── Ignore IDE & VCS metadata ───────────────────────────
.vscode/
.idea/
.git/
.gitignore

# ── Ignore Flutter/mobile build artifacts & caches ────
build/
.dart_tool/
.packages
.flutter-plugins*
.flutter-versions
.flutter-plugins-dependencies

# ── Ignore platform-specific code (Web only!) ───────────
android/
ios/
linux/
macos/
windows/

# ── Keep your app’s code & web entrypoint ──────────────
!pubspec.yaml
!pubspec.lock
!lib/**
!web/**
!assets/**             # if you have an assets/ folder referenced in pubspec.yaml

# ── Keep your Docker definitions ────────────────────────
!Dockerfile
!docker-compose.yml
!.dockerignore

# Anything else is useless for the container build
*
